Usage:
"grep.py keyword"
-Show the filepath to any instance of the keyword in a filename, dirname,
  or any line within a file. Starts in current directory and scans all sub directory's as well
"grep.py -t keyword"
  -the "-t" flag tells the program to start scanning from the users root directory
